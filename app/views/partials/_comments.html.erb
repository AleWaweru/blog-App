<div class="card bg-success">
  <div class="card-body">
    <% comments.each do |comment| %>
      <p><strong><%= comment.author.name %></strong>: <%= comment.text %></p>
    <% end %>
  </div>
</div>
